import React from 'react';
import styled from 'styled-components';

interface StatusInputProps {}

const StatusInput = ({}: StatusInputProps) => {
  return <Wrapper></Wrapper>;
};

const Wrapper = styled.div``;

export default StatusInput;
